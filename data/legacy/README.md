# Legacy artefacts (not paper truth)

Superseded numeric and figure artefacts kept only for historical comparison.

| Item | Notes |
|------|--------|
| `H{0,1,2}_0.999.mat` | Older margin endpoints; Algorithm-1 margins in the current toolbox match these to ~1e-15, but do not treat these files as the specification |
| `legacy_manuscript_figs/` | Pre-toolbox manuscript `.fig` layouts (controller-index / old sensitivity axis) |
| `sensitivity_bounds_*.fig` | Ancillary sensitivity-bound figure from earlier work |

Authoritative quantities are the paper equations and the deliverables in `results/lipschitz-margin-matlab/` / `results/lipschitz-margin-python/`.
