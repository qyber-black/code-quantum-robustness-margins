# Inherits matlab/miss_hit.cfg; only the line limit differs.
#
# The tests print diagnostic lines naming a field, a controller and both
# values, which run long and are worth more unwrapped than wrapped: a
# failure message that has to be reassembled from fragments is one nobody
# reads carefully.
line_length: 320
